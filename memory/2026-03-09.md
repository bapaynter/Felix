# Session Notes — 2026-03-09

## Model Config Changes

### Root Cause of qwen failures
- Config had `agents.defaults.model.primary` AND `agents.list[0].model` both set to `openrouter/z-ai/glm-5`
- Previous "fix" only updated some references, not the agent list entry
- **Fix applied**: Patched both `agents.defaults.model.primary` AND `agents.list[0].model` to `openrouter/qwen/qwen3.5-plus-02-15`
- Confirmed working in main session after restart

### Isolated session model failure (UNRESOLVED ROOT CAUSE)
- After switching primary to qwen, all isolated cron sessions fail: `Error: Unknown model: openrouter/qwen/qwen3.5-plus-02-15`
- Main session works fine with qwen; only isolated sessions fail
- Attempts that did NOT fix it:
  - Enabling `qwen-portal-auth` plugin
  - Adding qwen to global `models.providers.openrouter.models` catalog
- **Workaround applied**: Updated the 3 failing cron jobs to explicitly use `openrouter/deepseek/deepseek-v3.2` in payload model field
  - email-sorter
  - health-critical
  - health-resources
- This is a known OpenClaw bug or config behavior where isolated sessions don't resolve `openrouter/qwen/...` model IDs correctly

### Heartbeat model
- Changed from `openrouter/qwen/qwen3.5-flash-02-23` → `openrouter/stepfun/step-3.5-flash`
- (qwen flash likely has same isolated session resolution problem)

## Gateway Config Fix
- After a config patch, `gateway.controlUi.allowedOrigins` was missing the Tailscale URL
- Error: "origin not allowed" when connecting from Tailscale
- **Fix**: Added `https://e9ced02b-d0cc-4597-82c1-db5612705335.tail026133.ts.net` to `gateway.controlUi.allowedOrigins`
- **Lesson**: When patching gateway config, always preserve the Tailscale origin in allowedOrigins

## Current Config State
- Primary model: `openrouter/qwen/qwen3.5-plus-02-15` (main session ✅)
- Heartbeat model: `openrouter/stepfun/step-3.5-flash`
- Isolated cron jobs: use deepseek as workaround
- qwen-portal-auth plugin: enabled (may or may not matter)
- Tailscale origin added to allowedOrigins

## Open Questions
- Why do isolated sessions fail to resolve `openrouter/qwen/...` model IDs when main session works fine?
- Is this a version-specific OpenClaw bug? (running 2026.2.12, updated to 2026.3.8 during session)
